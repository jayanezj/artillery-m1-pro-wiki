![artillery-wiki-printer.svg](/media/images/wiki-repository-title.svg)

## 🇬🇧 English

This repository is associated with a Wiki.js instance – https://artillerym1.codigojose.com – so it can be used as a backup and collaboration system for the pages.

## Contribution

- If you want to improve or add the translation into any language:
  - Create a branch `feature/LANGUAGE` (for example: `feature/it`).
  - Add the translated pages.
  - Open a pull request to merge the changes.
- If you want to add a new entry:
  - First determine whether you will add a `guide`, `improvement` or `troubleshooting` entry. If you want to propose a new category, do so in the discussions section.
  - Create a branch `feature/new/NEW-ENTRY` (for example: `feature/new/bed-fix`)
  - Add the content of the new entry at least in English (you may add it in other languages as well).
  - Open a pull request to merge the changes.

## 🇪🇸 Español

Este repositorio está asociado a una instancia de Wiki.js – https://artillerym1.codigojose.com – de tal forma que se pueda utilizar como respaldo y sistema de colaboración para las páginas.

## Contribución

- Si quieres mejorar o añadir la traducción a algún idioma:
  - Crea una rama `feature/IDIOMA` (por ejemplo: `feature/it`).
  - Añade las páginas traducidas.
  - Realiza un pull request para integrar los cambios.
- Si quieres añadir una nueva entrada:
  - Determina primero si vas a añadir una entrada de `guía`, `mejora` o `resolución de problemas`. Si quieres proponer una nueva categoría hazlo en la sección de discusiones.
  - Crea una rama `feature/new/NUEVA-ENTRADA` (por ejemplo: `feature/new/bed-fix`)
  - Añade el contenido de la nueva entrada al menos en inglés (puedes añadirlo en otros idiomas también).
  - Realiza un pull request para integrar los cambios.

## 🇫🇷 Français

Ce dépôt est associé à une instance Wiki.js – https://artillerym1.codigojose.com – de sorte qu’il puisse être utilisé comme système de sauvegarde et de collaboration pour les pages.

## Contribution

- Si vous souhaitez améliorer ou ajouter la traduction dans une langue :
  - Créez une branche `feature/LANGUE` (par exemple : `feature/it`).
  - Ajoutez les pages traduites.
  - Faites une pull request pour intégrer les modifications.
- Si vous souhaitez ajouter une nouvelle entrée :
  - Déterminez d’abord si vous allez ajouter une entrée `guide`, `amélioration` ou `dépannage`. Si vous souhaitez proposer une nouvelle catégorie, faites-le dans la section discussions.
  - Créez une branche `feature/new/NOUVELLE-ENTRÉE` (par exemple : `feature/new/bed-fix`)
  - Ajoutez le contenu de la nouvelle entrée au moins en anglais (vous pouvez l’ajouter dans d’autres langues également).
  - Faites une pull request pour intégrer les modifications.

## 🇩🇪 Deutsch

Dieses Repository ist mit einer Wiki.js-Instanz – https://artillerym1.codigojose.com – verbunden, sodass es als Sicherungs- und Kollaborationssystem für die Seiten verwendet werden kann.

## Beitrag

- Wenn du eine Übersetzung in eine Sprache verbessern oder hinzufügen möchtest:
  - Erstelle einen Branch `feature/SPRACHE` (zum Beispiel: `feature/it`).
  - Füge die übersetzten Seiten hinzu.
  - Erstelle einen Pull Request, um die Änderungen zu integrieren.
- Wenn du einen neuen Eintrag hinzufügen möchtest:
  - Bestimme zuerst, ob du einen `guide`, `improvement` oder `troubleshooting` Eintrag hinzufügen wirst. Wenn du eine neue Kategorie vorschlagen möchtest, mache das im Diskussionsbereich.
  - Erstelle einen Branch `feature/new/NEUER-EINTRAG` (zum Beispiel: `feature/new/bed-fix`)
  - Füge den Inhalt des neuen Eintrags mindestens auf Englisch hinzu (du kannst ihn auch in anderen Sprachen hinzufügen).
  - Erstelle einen Pull Request, um die Änderungen zu integrieren.

